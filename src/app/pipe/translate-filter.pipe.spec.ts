import { TranslateFilterPipe } from './translate-filter.pipe';

describe('TranslateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
